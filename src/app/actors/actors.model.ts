export interface actorCreationDTO {
  name: string;
  dateOfBirth: Date;
  picture: string | File;
  biography: string;
}

export interface actorDTO {
  name: string;
  dateOfBirth: Date;
  picture: string | File;
  biography: string;
}
